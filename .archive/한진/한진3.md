한진 택배 API SPEC
[RESTful API]
2025.10
한진
1
목 차

1. API 개요
2. REST API 레퍼런스
3. 계약번호 검증
   2
4. API 개요
   • 운영 : https://api.hanjin.com/v1
   • 개발 : https://api-stg.hanjin.com/v1
   1.1 API 사용을 위한 인증(Authorization)

- API 사용을 위해서는 Client id, Auth Key(인증키)를 발급 및 HMAC Signature 생성 정보가 필요하며, 관련 내용은 담당자에게 별도 전달 예정

* 참조 - HMAC (Hash-based Message Authentication Code)는 송신자와 수신자만 공유하고 있는 Key와 Message를 혼합하여
  Hash 값을 생성 / 비교 – 데이터의 무결성, 신뢰성을 보장함.

- HMAC Signature 생성

1. HmacSHA256 방식을 통해 Hash 값 생성
2. Hash 값에 이용되는 Message 내용 :
   timeStamp(yyyyMMddHHMMss), Method, Query Parameter, Auth key(SecurityKey)를 합친 문자열로 생성
   (ex) “20230730121212GETA=A&B=BKEY값”
3. Hash값은 Hex 문자열로 변환
4. 생성된 Hash 값 및 client_id, timestamp를 포함한 데이터를 Header에 Authorization으로 추가하여 호출
   ex) client_id=HANJIN timestamp=20230730121212 signature=HASH값 🡪 각 값의 사이에는 공백 포함

- Request Header

1. Content-Type : application/json
2. x-api-key : 발급된 Key를 전달
3. Authorization : 발급된 Secret key를 사용하여 HMAC Signature 를 전달
   3

2) REST API 레퍼런스
   2.1 REST API 목록
   https://api.hanjin.com/parcel-delivery/v1 URL
   https://api-stg.hanjin.com/parcel-delivery/v1 순번 서비스명 PATH 1 계약번호 검증 POST /customer/customer-check 계약번호 검증
   4
   운 영
   개 발
   비 고
3) REST API 레퍼런스
   2.2 응답 코드

- 응답 코드는 요청에 대한 상태를 나타내는 HTTP의 상태 코드(Status code)와 에러에 대한 정보를 담은 에러코드(Error Code)로 안내됩니다.
- HTTP 상태 코드(HTTP Status Code)란 요청에 대한 상태 정보(성공 또는 실패)를 나타내며, 아래 표를 참조바랍니다.
  상태 코드 상태 설명
  200
  OK
  서버가 클라이언트의 요청을 성공적으로 수행
  성공
  응답 바디의 경우 각 API별 상세 설명을 참조
  400
  Bad Request
  일반적인 오류
  실패
  주로 API 필수 파라미터와 관련 서버가 클라이언트 오류를 감지해 요청 처리 불가한 상태
  401
  Unauthorized
  인증오류
  실패
  해당 리소스에 유효한 인증 자격 증명이 없어 요청에 실패한 상태
  403
  Forbidden
  권한 오류
  실패
  서버에 요청이 전달되었지만, 권한 문제로 거절된 상태
  429
  Too Many Request
  호출한도 초과 오류
  실패
  정해진 사용량이나 초당 요청 한도를 초과한 경우
  500
  Internal Server Error
  시스템 오류
  실패
  서버가 예상하지 못한 에러 상태
  503
  Service Unavailable
  서비스 점검중
  실패
  서버가 요청을 처리할 준비가 되지 않은 상태
  5

2. REST API 레퍼런스
   2.3 에러 코드

- 요청이 실패하였을 경우, 에러코드(error_code) 와 메시지(message)로 반환됩니다.
- 실패 메세지를 나타내는 msg는 의미가 바뀌지 않는 범위에서 내용이 변경될 수 있으며, message 내용은 다양하게 전달됩니다.

# 에러코드

errorCode 설명 message 예시 1) Unauthorized Key(signature 오류) 2) Unauthorized Key(client_id 오류)
인증키(key) 또는 허용된 아이피정보가 맞지 않을 경우
-101 3) Unauthorized IP
해결방법 : Key 또는 허용된 아이피 정보 확인후 재시도 4) Unauthorized Key(timestamp 만료) 5) InvalidApiKeyForGivenResource(권한 불충분)
필수 입력값, 입력값의 데이터 타입이 적절하지 않거나 허용된 범위를 벗어난 경우

1. Bad Request - 유효하지 않은 요청
   -102
   해결방법 : 요청 파라미터 확인
2. Bad Request - 유효하지 않은 요청(client_id)
   -103 정해진 사용량이나 초당 요청 한도를 초과한 경우 Too many request -500 시스템 오류 Internal Server Error -503 서비스 점검중 시스템 작업중입니다. -504 최대 30초를 초과한 경우 Gateway Timeout # 예시 : 인증키(key) 오류
   HTTP/1.1 403 Forbidden
   {
   “errorCode”: -101,
   “message”: "Unauthorized Key(Authorization 오류)"
   }
   6
   HTTP
   상태 코드
   403
   400
   429
   500
   503
   504

3) 계약번호 검증 (customer-check)
   3.1 Request
   PATH GET /customer/customer-check
   Example Endpoint https://api.hanjin.com/parcel-delivery/v1/customer/customer-check?cntractNo=1234&custBizNo=5678 (운영)
   https://api-stg.hanjin.com/parcel-delivery/v1/customer/customer-check?cntractNo=1234&custBizNo=5678 (개발)
   QUERY STRING PARAMETER
   순번 필드 영문명 한글명 TYPE LENGTH NULLABLE 비 고
   1 cntractNo 계약번호 VARCHAR2 7 고객사 계약번호(신용번호)
   2 custBizNo 사업자등록번호 VARCHAR2 10 O 사업자번호(옵션)
   ※ 사업자번호 입력시 사업자 번호 & 계약번호 검증, 계약번호만 입력시 계약번호로만 검증
   7
4) 계약번호 검증 (customer-check)
   3.2 Response

- API의 처리 결과는 결과코드(resultCode)를 참조바랍니다.
- 처리결과 메세지(resultMessage)는 의미가 바뀌지 않는 범위에서 내용이 변경될 수 있으며, 결과메세지(resultMessage) 내용은 다양하게 전달됩니다.
  PATH GET /customer/customer-check
  RESPONSE BODY
  순번 필드 영문명 한글명 TYPE LENGTH NULLABLE 비 고
  1 resultCode 결과코드 VARCHAR2 8 NOT NULL 서비스 처리에 대한 결과코드 / <표1> 결과 코드 참조
  2 resultMessage 결과메세지 VARCHAR2 200 NOT NULL
  3.3 Response
  <표1> 고객번호 검증 결과
  resultCode resultMessage 비 고
  OK SUCCESS 사업자 번호 입력시 사업자 번호와
  계약번호를 같이 검증하여 결과값 도출
  ERROR-01 계약번호 거래 중지
  ERROR-02 올바르지 않은 계약번호
  ERROR-99 기타 오류
  8
